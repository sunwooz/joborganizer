
= form_for([current_user, @job_detail], remote: true, namespace: 'cover-letter') do |f|
  = f.text_area :cover_letter, class: "tinymce", id: "cover-letter-#{@job_detail.id}"
  = f.submit "Save", id: "cover-letter-save"

:javascript
  $(document).ready(function() {
    $('a#interested').hide();
    tinyMCE.editors=[]
    tinyMCE.init({ 
      selector: '.tinymce',
      height: 300
    });

  });
  